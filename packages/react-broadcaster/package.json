{
  "name": "react-broadcaster",
  "version": "0.0.1",
  "description": "React implementation of Broadcaster package. Extension of a Broadcast Channel API. Allows to send messages accross multiple windows (tabs, workers, iframes,...) and detect opened windows.",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "clean": "npx rimraf ./dist",
    "prepublish": "npm run clean && npx tsc",
    "watch": "npx tsc --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pavelstencl/broadcaster.git"
  },
  "keywords": [
    "broadcast",
    "channel",
    "BroadcastChannel",
    "react",
    "hook",
    "share",
    "state",
    "tabs",
    "windows",
    "browser"
  ],
  "author": "Pavel Stencl",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/pavelstencl/broadcaster/issues"
  },
  "homepage": "https://github.com/pavelstencl/broadcaster/packages/react-broadcaster#readme",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.7.0",
		"@typescript-eslint/parser": "^6.7.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
